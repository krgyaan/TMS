GET    leads/{lead_id}/followup          # List lead_followups
GET    leads/{lead_id}/followup/{id}     # Get one lead_followup
POST   leads/{lead_id}/followup          # Create a lead_followup
PUT    leads/{lead_id}/followup/{id}     # Replace a lead_followup
PATCH  leads/{lead_id}/followup/{id}     # Partially update a lead_followup
DELETE leads/{lead_id}/followup/{id}     # Delete a lead_followup