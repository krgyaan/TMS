<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bank Guarantee Indicative Document 2</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body {
            font-family: 'Times New Roman', serif;
            font-size: 12pt;
            line-height: 1.6;
            color: #000;
            padding: 20mm 15mm;
        }
        .header {
            text-align: center;
            margin-bottom: 30px;
            border-bottom: 2px solid #000;
            padding-bottom: 15px;
        }
        .header h1 {
            font-size: 18pt;
            font-weight: bold;
            margin-bottom: 5px;
        }
        .content {
            margin: 20px 0;
        }
        .section {
            margin-bottom: 20px;
        }
        .section-title {
            font-weight: bold;
            font-size: 13pt;
            margin-bottom: 10px;
            text-decoration: underline;
        }
        .field-row {
            margin-bottom: 8px;
            display: flex;
        }
        .field-label {
            font-weight: bold;
            min-width: 180px;
        }
        .field-value {
            flex: 1;
        }
        .footer {
            margin-top: 30px;
            font-size: 10pt;
            text-align: center;
            color: #666;
        }
        @media print {
            body {
                padding: 0;
            }
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>BANK GUARANTEE INDICATIVE DOCUMENT - PART 2</h1>
    </div>

    <div class="content">
        <div class="section">
            <p>This document provides additional indicative information for the Bank Guarantee request.</p>
        </div>

        {{#if bgDetails}}
        <div class="section">
            <div class="section-title">Bank and Beneficiary Details</div>
            {{#if bgDetails.bankName}}
            <div class="field-row">
                <span class="field-label">Bank Name:</span>
                <span class="field-value">{{bgDetails.bankName}}</span>
            </div>
            {{/if}}
            {{#if bgDetails.beneficiaryName}}
            <div class="field-row">
                <span class="field-label">Beneficiary Name:</span>
                <span class="field-value">{{bgDetails.beneficiaryName}}</span>
            </div>
            {{/if}}
            {{#if bgDetails.beneficiaryAddress}}
            <div class="field-row">
                <span class="field-label">Beneficiary Address:</span>
                <span class="field-value">{{bgDetails.beneficiaryAddress}}</span>
            </div>
            {{/if}}
            {{#if bgDetails.bgBankAcc}}
            <div class="field-row">
                <span class="field-label">Account Number:</span>
                <span class="field-value">{{bgDetails.bgBankAcc}}</span>
            </div>
            {{/if}}
            {{#if bgDetails.bgBankIfsc}}
            <div class="field-row">
                <span class="field-label">IFSC Code:</span>
                <span class="field-value">{{bgDetails.bgBankIfsc}}</span>
            </div>
            {{/if}}
            {{#if bgDetails.stampCharges}}
            <div class="field-row">
                <span class="field-label">Stamp Charges:</span>
                <span class="field-value">{{formatCurrency bgDetails.stampCharges}}</span>
            </div>
            {{/if}}
        </div>
        {{/if}}

        {{#if instrument}}
        <div class="section">
            <div class="section-title">Validity Period</div>
            {{#if instrument.expiryDate}}
            <div class="field-row">
                <span class="field-label">Expiry Date:</span>
                <span class="field-value">{{formatDate instrument.expiryDate}}</span>
            </div>
            {{/if}}
            {{#if instrument.claimExpiryDate}}
            <div class="field-row">
                <span class="field-label">Claim Expiry Date:</span>
                <span class="field-value">{{formatDate instrument.claimExpiryDate}}</span>
            </div>
            {{/if}}
        </div>
        {{/if}}

        <div class="section">
            <p><strong>Note:</strong> This is an indicative document. Final Bank Guarantee will be issued by the bank.</p>
        </div>
    </div>

    <div class="footer">
        <p>Generated on {{formatDateTime generatedAt}}</p>
    </div>
</body>
</html>
