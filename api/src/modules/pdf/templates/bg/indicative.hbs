<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bank Guarantee Indicative Document</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body {
            font-family: 'Times New Roman', serif;
            font-size: 12pt;
            line-height: 1.6;
            color: #000;
            padding: 20mm 15mm;
        }
        .header {
            text-align: center;
            margin-bottom: 30px;
            border-bottom: 2px solid #000;
            padding-bottom: 15px;
        }
        .header h1 {
            font-size: 18pt;
            font-weight: bold;
            margin-bottom: 5px;
        }
        .content {
            margin: 20px 0;
        }
        .section {
            margin-bottom: 20px;
        }
        .section-title {
            font-weight: bold;
            font-size: 13pt;
            margin-bottom: 10px;
            text-decoration: underline;
        }
        .field-row {
            margin-bottom: 8px;
            display: flex;
        }
        .field-label {
            font-weight: bold;
            min-width: 180px;
        }
        .field-value {
            flex: 1;
        }
        .footer {
            margin-top: 30px;
            font-size: 10pt;
            text-align: center;
            color: #666;
        }
        @media print {
            body {
                padding: 0;
            }
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>BANK GUARANTEE INDICATIVE DOCUMENT</h1>
    </div>

    <div class="content">
        <div class="section">
            <p>This document serves as an indicative reference for the Bank Guarantee request.</p>
        </div>

        <div class="section">
            <div class="section-title">Bank Guarantee Details</div>
            <div class="field-row">
                <span class="field-label">Request ID:</span>
                <span class="field-value">{{request.id}}</span>
            </div>
            <div class="field-row">
                <span class="field-label">Instrument ID:</span>
                <span class="field-value">{{instrument.id}}</span>
            </div>
            <div class="field-row">
                <span class="field-label">Amount:</span>
                <span class="field-value">{{formatCurrency instrument.amount}}</span>
            </div>
            <div class="field-row">
                <span class="field-label">Favouring:</span>
                <span class="field-value">{{instrument.favouring}}</span>
            </div>
            {{#if bgDetails}}
            {{#if bgDetails.bgPurpose}}
            <div class="field-row">
                <span class="field-label">Purpose:</span>
                <span class="field-value">{{bgDetails.bgPurpose}}</span>
            </div>
            {{/if}}
            {{#if bgDetails.bgNeeds}}
            <div class="field-row">
                <span class="field-label">Required In:</span>
                <span class="field-value">{{bgDetails.bgNeeds}}</span>
            </div>
            {{/if}}
            {{/if}}
        </div>

        {{#if tender}}
        <div class="section">
            <div class="section-title">Tender Information</div>
            <div class="field-row">
                <span class="field-label">Tender No:</span>
                <span class="field-value">{{tender.tenderNo}}</span>
            </div>
            {{#if tender.tenderName}}
            <div class="field-row">
                <span class="field-label">Project Name:</span>
                <span class="field-value">{{tender.tenderName}}</span>
            </div>
            {{/if}}
        </div>
        {{/if}}

        <div class="section">
            <p><strong>Note:</strong> This is an indicative document. Final Bank Guarantee will be issued by the bank.</p>
        </div>
    </div>

    <div class="footer">
        <p>Generated on {{formatDateTime generatedAt}}</p>
    </div>
</body>
</html>
